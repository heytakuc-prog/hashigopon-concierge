# はしごポン 実店舗版実装仕様

## 目的

イベント参加者が、はしごポンとの8問の会話を楽しみながら、回答に合う実店舗候補を最大3件見つけられるスマートフォン向けWebアプリとする。

推薦精度だけでなく、キャラクターとの会話、街への回遊、安全な飲酒案内を重視する。

## 会話フロー

通常ルートは次の8問とする。安全上の理由で終了する場合を除き、店の雰囲気だけでなく、何軒目か、具体的な好み、移動可能範囲まで確認する。

1. 今どんな気分？
2. 何人？
3. 今、何軒目？
4. 予算はどのくらい？
5. どんな雰囲気の店がいい？
6. 食べたい？ 飲みたい？
7. 気になる料理・お酒は？
8. どこまで移動できる？

8問の回答後、1.35秒の考え中演出を挟み、候補を最大3件表示する。自由入力は必須にせず、結果画面の「条件を一言足す」から任意で使う。

## 安全ルート

「かなり酔ってる」を選んだ場合は店舗を推薦しない。

- 「もう家に帰れ！」とツッコむ
- 水分補給、休憩、安全な帰宅を案内する
- 松戸駅の地図を表示する
- 最初からやり直す操作だけを残す

## 推薦ロジック

ユーザー回答をタグ化し、店舗タグとの一致を点数化する。特に店舗アンケート由来の情報を重視する。

- 店舗コメント由来タグ: 11点
- 歓迎客タグ: 10点
- 人数タグ: 8点
- 気分・雰囲気タグ: 7点
- 食事タグ: 6点
- その他の補助タグ: 3〜4点

予算、1人利用、大人数、食事重視、盛り上がり、何軒目、具体的な料理・酒の好み、移動距離にも追加点を設定する。上位5件から最大3件を選ぶため、再診断や「別の3軒」で候補が多少変わる。

画面には数値の相性パーセントを出さず、「かなり合いそう」「意外とアリ」「気まぐれ枠」と表示する。

## 営業時間判定

- 営業時間内だけ営業中加点する
- 営業時間前は `open=false / state=before` とする
- 営業終了、本日休みは減点する
- `25:00` など翌日表記と、前日から続く深夜営業を判定する

営業時間は変更される可能性があるため、店舗カードでは確認用情報として扱う。

## イベントゾーン

HTMLの `data-event-zone="matsudo_station"` とCSVの `event_zone` を照合する。「駅から近い方がいい」「10分くらいなら歩く」では `matsudo_station` の店舗だけを候補とする。「遠くても良い店なら行く」を選んだ場合だけ全ゾーンを解放し、`yagiri` のYAGIRIYAにも距離加点を与える。

## 店舗カード

表示項目は次の通り。

- 店名、ジャンル、店舗画像
- はしごポンの推薦理由
- 店舗アンケートまたは公開情報からの特徴
- 会場からの徒歩分数・アクセス補足
- 本日の営業時間と現在状態
- 予算、イベント特典
- Googleマップ、Instagram

`data_source` に応じて「お店からのひとこと」「公開情報からの特徴」「デモ情報」を明示する。

## CSV追加列

既存列に次を追加する。

| 列名 | 内容 | 例 |
|---|---|---|
| `event_zone` | イベント対象エリア | `matsudo` |
| `area` | 表示用エリア | `松戸駅西口` |
| `walk_minutes` | 会場からの徒歩分数 | `4` |
| `access_note` | 補足アクセス | `西口・駅近` |
| `data_source` | 情報源 | `survey` / `public` / `demo` |

## 情報源の扱い

- `survey`: 店舗アンケート回答。歓迎客・雰囲気・コメントを強く推薦へ反映する
- `public`: WebやSNSの公開情報から暫定登録。店舗の公式回答とは表現しない
- `demo`: 試作用店舗。公開本番では実店舗データへ置換する

## 現在の店舗データ

GitHubの `main` ブランチから実店舗12件のCSVを取得し、ローカル版へ統合済み。

- 店舗アンケート確認済み: 6件
- 公開情報による暫定データ: 6件
- 松戸駅イベントの通常候補: 11件
- 別ゾーン: YAGIRIYA 1件（`event_zone=yagiri`。「遠くても良い店なら行く」で候補に含める）

## 公開前確認

- 実店舗名、営業時間、予算、特典の確認
- 店舗固有のGoogleマップURL、Instagram URLの登録
- 店舗写真の利用許諾
- 近距離回答ではYAGIRIYAが出ず、遠距離回答では候補になり得ることを確認
- スマートフォン幅360pxと430pxでの表示確認
- LINEから開いたブラウザでの動作確認

